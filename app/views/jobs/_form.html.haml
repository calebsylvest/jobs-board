= simple_form_for(@job, html: { class: 'form-horizontal' }) do |f|
  = f.input :title, label: "Job Title"
  = f.input :company, label: "Company"
  = f.input :url, label: "Link to Job Listing"
  %br/
  = f.submit :submit